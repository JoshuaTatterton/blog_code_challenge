<div class="container">

  <div class="article_title" ><%= @article.title %></div>

  <%= render partial: "article", locals: { article: @article } %>

</div>

<button class="btn" id="comment">Comment</button>

<div class="commenter container">
  <%= form_for [@blogger, @article, @comment] do |f| %>
    <div>
      <%= f.label :name %>:
      <%= f.text_field :name %>
    </div>
    <div>  
      <%= f.label :comment %>:
      <%= 
      f.text_area(:content, size: "48x6") %>
    </div>
    <%= f.submit "Post Comment", class: "btn" %>
  <% end %>
</div>

<div>
  <% @comments.each do |comment| %>
    <div>
      <%= comment.name %>: <%= comment.content %>
      commented on: <%= comment.created_at.strftime("%H:%M on %e/%-m/%y") %>
      <% if current_user == @blogger %>
        <%= link_to "Delete Comment", blogger_article_comment_path(@blogger, @article, comment), method: :delete, class: "btn" %>
      <% end %>
    </div>
  <% end %>
</div>