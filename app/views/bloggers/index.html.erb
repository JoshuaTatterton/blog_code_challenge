<div>
  Welcome to the BlogSite.
  Either check out the most recent blogs or start your own.
</div>
<% if @articles != [] %>

  <% @articles.each do |article| %>

    <%= link_to blogger_article_path(article.blogger, article), id: article.id do %>
      <%= article.title %>
      <%= article.content %>
      -<%= article.blogger.username %>
    <% end %>

  <% end %>

<% else %>

  <div class="container">
    <div>Oops there are no articles here.</div>

    <div>Why don't you become a blogger and write the first one.</div>
  </div>

<% end %>